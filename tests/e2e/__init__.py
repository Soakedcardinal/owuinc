"""End-to-end tests - production integration with real OpenWebUI + Nextcloud.

These tests verify the full stack works correctly but require:
- Live OpenWebUI instance
- Live Nextcloud instance
- Proper configuration of valves

Running these is slower (minutes) and should be done manually before releases,
not on every CI run.
"""
